function love.conf(t)
    t.window.fullscreen = true          -- Definir para tela cheia
    t.window.fullscreentype = "desktop" -- Tipo de tela cheia (pode ser "desktop" também)
    t.window.width = 0                  -- Define a largura como 0 para que seja automática
    t.window.height = 0
end
